# my-webapp-api-tests
This project is to test Hackernews API (public)
Dev Environment setup:
   python3 -m venv venv
   source venv/bin/activate (this will create virtual env)
   (venv) pip install -r requirements.txt
How to run tests:
    pytest <test_file>
How to view results:
    
